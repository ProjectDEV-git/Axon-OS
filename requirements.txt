# Axon OS Python dependencies
# Note: Many of these require system bindings and are best installed via apt-get on Ubuntu:
# sudo apt-get install python3-dbus python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1 python3-vte-2.91

# Core D-Bus and GObject bindings (system-installed on Ubuntu, pinned for stability)
dbus-python==1.3.2
PyGObject==3.48.2

# Optional: Ollama client (if building HTTP client)
# requests>=2.31.0
